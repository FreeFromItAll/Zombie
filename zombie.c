#include <stdio.h>
#include <string.h>

int main(const int argc, const char** argv)
{
	if(argc < 2) {
usage:
		puts("usage: zombie [-i|-f] file");
		exit(1);
	}

	puts("Zombie Script Interpreter v1.3.1");
	puts("--------------------------------");

	
}